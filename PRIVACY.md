# LinkTap 隐私说明

生效日期：2026-09-15

LinkTap 的设计目标是以最少的数据处理完成链接复制功能。

## 收集的数据

LinkTap 不收集、出售或上传个人信息、浏览记录、页面内容、剪贴板内容或点击历史。

插件只会在用户主动触发所配置的修饰键组合时读取被点击链接的 URL，并将该 URL 写入系统剪贴板。URL 不会被保存或发送到任何服务器。

## 保存的设置

LinkTap 使用 `chrome.storage.sync` 保存以下内容：

- 插件是否启用。
- 用户选择的修饰键组合。

这些偏好可能通过用户自己的 Chrome Sync 在其设备间同步。LinkTap 的开发者无法访问这些设置。

## 网络与第三方服务

LinkTap 不包含广告、分析工具、追踪器、远程代码或外部网络请求，也不与第三方服务共享数据。

## 权限用途

- 网页访问权限：在普通网页中检测链接点击。
- `scripting`：为已经打开的标签页注入 LinkTap 自带的本地内容脚本。
- `clipboardWrite`：将用户触发的链接 URL 写入剪贴板。
- `storage`：保存插件状态和快捷键偏好。

## 变更

如本隐私说明发生重大变化，将在项目的 Release Notes 和 `CHANGELOG.md` 中说明。
